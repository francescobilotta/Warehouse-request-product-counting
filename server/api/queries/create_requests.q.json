{
  "database": "portale",
  "query": "INSERT INTO testing.Request (requestDate, productCode, productName, productFlavour, dueDate, requestState, notes) VALUES (\"{d.requestDate}\", \"{d.productCode}\", \"{d.productName}\", \"{d.productFlavour}\", \"{d.dueDate}\", \"{d.requestState}\", \"{d.notes}\")"
}
