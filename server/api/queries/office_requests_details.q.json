{
    "database": "portale",
    "query": "SELECT requestId, productCode, productName, productFlavour, notes, requestDate, dueDate, requestState, previousCount, lastCount, isClosed, expectedCount FROM testing.Request WHERE requestId = \"{f.requestId}\""
}
