{
    "database": "portale",
    "query": "SELECT requestId, requestState, productCode, productName, productFlavour, notes, requestDate, dueDate, terminationDate, previousCount, lastCount, isClosed, expectedCount FROM testing.Request WHERE isClosed = '0' ORDER BY dueDate"
}
