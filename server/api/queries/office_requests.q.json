{
  "name": "office_requests",
  "database": "portale",
  "schema": "testing",
  "query": "SELECT requestId, productCode, productName, productFlavour, notes, requestDate, dueDate, requestState, isClosed, expectedCount, previousCount, lastCount, terminationDate, expectedGroundCount FROM testing.Request ORDER BY requestId"
}