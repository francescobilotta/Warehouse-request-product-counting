{
  "database": "portale",
  "query": "SELECT requestId, requestState, productName, productFlavour, notes, requestDate, dueDate, terminationDate, previousCount, lastCount, isClosed FROM testing.Request ORDER BY dueDate"
}
