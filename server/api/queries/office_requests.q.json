{
    "database": "portale",
    "query": "SELECT requestId, requestState, productCode, productName, productFlavour, notes, requestDate, dueDate, terminationDate, previousCount, lastCount, isClosed, expectedCount FROM testing.Request ORDER BY dueDate"
}
