{
  "name": "warehouse_requests_details",
  "database": "portale",
  "schema": "testing",
  "dialect": "mysql",
  "query": "SELECT requestId, requestState, productCode, productName, productFlavour, notes, requestDate, isClosed, dueDate, expectedCount, expectedGroundCount FROM testing.Request WHERE requestId = '{f.requestId}'"
}
