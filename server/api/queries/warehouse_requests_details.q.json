{
  "database": "portale",
  "query": "SELECT requestId, productCode, productName, productFlavour, notes, requestDate, dueDate, expectedCount, expectedGroundCount FROM testing.Request WHERE requestId = '{f.requestId}'"
}
